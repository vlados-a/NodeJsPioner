var phrase = require('./ru'),
	log = require('logger')(module);

function User(name){
	this.name = name;
}

User.prototype.hello = function(who){
	log(phrase.Hello + ", " + who.name);
};

module.exports = User;